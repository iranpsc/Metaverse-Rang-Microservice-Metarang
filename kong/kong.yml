_format_version: "3.0"
_transform: true

services:
  # Static uploaded files (profile photos, KYC, etc.) — not API calls
  - name: storage-uploads
    url: http://storage-service:8059
    protocol: http
    host: storage-service
    port: 8059
    connect_timeout: 60000
    write_timeout: 60000
    read_timeout: 60000
    retries: 3

  - name: grpc-gateway
    url: http://grpc-gateway:8080
    protocol: http
    host: grpc-gateway
    port: 8080
    path: /
    connect_timeout: 60000
    write_timeout: 60000
    read_timeout: 60000
    retries: 5

routes:
  # Higher priority: serve files from storage-service, never grpc-gateway
  - name: uploads-static
    service: storage-uploads
    paths:
      - /uploads
    strip_path: false
    preserve_host: false
    regex_priority: 100
    protocols:
      - http
      - https
    methods:
      - GET
      - HEAD
      - OPTIONS

  - name: api-routes
    service: grpc-gateway
    paths:
      - /api
    strip_path: false
    preserve_host: false
    protocols:
      - http
      - https
    methods:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      - OPTIONS
    regex_priority: 0

plugins:
  - name: prometheus
    config: {}
  - name: cors
    config:
      origins:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Accept-Language
        - Content-Language
        - Content-Type
        - Authorization
        - X-Requested-With
      exposed_headers:
        - X-Auth-Token
      credentials: true
      max_age: 3600
      preflight_continue: false
