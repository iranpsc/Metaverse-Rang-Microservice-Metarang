# App Information
APP_URL=http://localhost:8000
PROJECT_LOCALE=EN
# Used for category/subcategory/video image and video URLs: ADMIN_PANEL_URL + /uploads/ + path
ADMIN_PANEL_URL=https://admin.metarang.com

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_DATABASE=metarang_db

# gRPC Server Configuration
GRPC_PORT=50057

# Auth Service Configuration (for user data retrieval)
AUTH_SERVICE_ADDR=auth-service:50051
